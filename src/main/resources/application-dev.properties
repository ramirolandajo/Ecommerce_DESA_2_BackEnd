# Profile: dev (H2 deshabilitado)

# spring.datasource.url=jdbc:h2:mem:ecommerce;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Ejecutar data.sql después de crear el schema JPA
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# H2 console (opcional)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Reusar el resto de configuración (Kafka, Keycloak, middleware) del application.properties.
# Solo se overridean las propiedades del datasource en este profile.